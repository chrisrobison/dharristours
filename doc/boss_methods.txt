
getObject($resource, $id, $recurse, $parent)
cleanObject($data, $clean, $wildphone)
searchObject($resource, $search, $parent, $grandparent, $sort)
makeCondition($rsc, $arr, $join='AND')

getRelationships($resource, $seen=array())
getRelated($resource, $id)

storeObject($data, $parent, $parentID)
clampRecord($local, $localID, $remote, $remoteID, $doReverse=false)

addModule($module)
addProcess($module, $process)
addAction($process, $action)

add($resource, $pid, $rid)
addResource($rcs)
setResource($resource)

buildForm($rcs, $data=array(), $tpl)
_quote($txt)

_Module($moduleID, $parent, $db)
    get()
    setModuleID($mid)
    getModule()
    addUtils()
    addProcess($pid)

_Process($moduleID, $process, $db)
    get($pid)
    getProcessChildren($pid)
    listActions($pid)
    setModuleID($mid)
    getModule()
    setProcess($pid)
    getProcess()
    getNextProcess($cid)
    addUtils()
    prepare()
    complete()
    addAction($pid)

_Action($process, $action, $db)
    get($aid)
    setAction($pid)
    getAction()
    setProcess($pid)
    getProcess()
    addUtils()
    prepare()
    complete()
    execute()
