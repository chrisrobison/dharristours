CREATE TABLE tmp/dispensary (
	ClubID INT(50) NOT NULL AUTO_INCREMENT PRIMARY KEY,
	Club VARCHAR(100) NOT NULL DEFAULT '',
	Address VARCHAR(50) NOT NULL DEFAULT '',
	CrossStreets VARCHAR(50) NOT NULL DEFAULT '',
	Phone VARCHAR(100) NOT NULL DEFAULT '',
	Hours TIME NOT NULL)

